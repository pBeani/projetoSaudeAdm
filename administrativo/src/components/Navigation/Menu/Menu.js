import React from 'react';
import classes from './Menu.css';

const Menu = props => {

    return (
        <nav className={classes.Menu}>
            <div>Menu item</div>
        </nav>
    )
};

export default Menu;